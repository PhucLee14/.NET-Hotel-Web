@model IEnumerable<WebApplication5.Models.DichVu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenDichVu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GiaDichVu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenDichVu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GiaDichVu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaDichVu }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaDichVu }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaDichVu })
        </td>
    </tr>
}

</table>
