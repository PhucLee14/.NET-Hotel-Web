@model IEnumerable<WebApplication5.Models.LoaiPhong>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Giá loại phòng
        </th>
        <th>
            Hình ảnh
        </th>
        <th>
           Tên Loại Phòng
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GiaLoaiPhong)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HinhAnh)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoaiPhong)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLoaiPhong }, new { @class = "acction-link" }) 
            @Html.ActionLink("Details", "Details", new { id=item.MaLoaiPhong }, new { @class = "acction-link" }) 
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLoaiPhong }, new { @class = "acction-link" })
        </td>
    </tr>
}

</table>
